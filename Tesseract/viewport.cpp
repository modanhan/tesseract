#include "aerobox.h"

namespace aerobox {
	glm::vec3 viewport_position(4,3,5), viewport_lookat(0,0,0), viewport_up(0,1,0);
	int viewport_width=0, viewport_height=0;
	float fov=45, viewport_near=.1, viewport_far=50;
}
